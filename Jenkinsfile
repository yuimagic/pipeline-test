pipeline {
   agent any

   stages {
      stage('build_Init') {
         steps {
            echo 'build init step'
         }
      }
      
       stage('build') {
         steps {
            echo 'build step'
         }
      }
      
       stage('test') {
         steps {
            echo 'test step'
         }
      }
      
   }
}
